services:
  goway:
    build: .
    ports:
      - "8080:8080"
    environment:
      - API_KEYS=${API_KEYS}
      - CONCURRENCY_LIMIT=${CONCURRENCY_LIMIT}
      - DEBUG=${DEBUG}
      - TEMPLATE_DIR=${TEMPLATE_DIR}
      - PORT=${PORT}
    restart: unless-stopped
